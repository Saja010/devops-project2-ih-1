# Server Configuration
server.port=8080

# Database Configuration (PostgreSQL Flexible Server)
spring.datasource.url=jdbc:postgresql://${DB_HOST:saja-postgres-db.postgres.database.azure.com}:${DB_PORT:5432}/${DB_NAME:burgerbuilder}
spring.datasource.username=${DB_USERNAME:pgadmin@# Server Configuration
server.port=8080

# Database Configuration (PostgreSQL Flexible Server)
spring.datasource.url=jdbc:postgresql://${DB_HOST:saja-postgres-db.postgres.database.azure.com}:${DB_PORT:5432}/${DB_NAME:burgerbuilder}?sslmode=require
spring.datasource.username=${DB_USERNAME:pgadmin@saja-postgres-db}
spring.datasource.password=${DB_PASSWORD:SajaNewP@ssw0rd123}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Initialize database with schema and data
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true
spring.sql.init.schema-locations=classpath:schema-postgresql.sql
spring.sql.init.data-locations=classpath:data-postgresql.sql

# Logging Configuration
logging.level.com.burgerbuilder=INFO
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
}
spring.datasource.password=${DB_PASSWORD:SajaStrongP@ssw0rd}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Initialize database with schema and data
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true
spring.sql.init.schema-locations=classpath:schema-postgresql.sql
spring.sql.init.data-locations=classpath:data-postgresql.sql

# Logging Configuration
logging.level.com.burgerbuilder=INFO
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE


